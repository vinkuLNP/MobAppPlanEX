enum SubscriptionPlan { free, pro }
enum ThemeType { light, dark }
enum ActiveField { email, password, none }
ActiveField activeField = ActiveField.none;
ActiveField lastActiveField = ActiveField.none;
